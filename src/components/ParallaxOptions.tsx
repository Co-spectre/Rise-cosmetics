import React from 'react';

// Simplified placeholder component for better performance
const ParallaxOptions = () => {
  return null; // This component is not used in the optimized landing page
};

export default ParallaxOptions;